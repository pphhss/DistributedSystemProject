import sys
sys.path.insert(0,"../")

from ds import ip

ip = ip.ip
listenPort = 8888
numNode = 4

db={
    'host' : 'localhost',
    'user' : 'root',
    'password' : 'root',
    'db' : 'ds',
    'charset' : 'utf8'
        }
